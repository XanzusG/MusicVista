# ===========================================
# MusicVista Client 环境变量配置示例
# ===========================================
# 
# Vite 环境变量说明：
# - 必须以 VITE_ 开头才能在客户端访问
# - 开发环境：复制此文件为 .env.development
# - 生产环境：复制此文件为 .env.production
# 
# Vite 加载规则：
# - npm run dev   → 自动加载 .env.development
# - npm run build → 自动加载 .env.production
# ===========================================

# 应用配置
VITE_APP_NAME=MusicVista
VITE_APP_VERSION=1.0.0
VITE_ENVIRONMENT=development        # development | production

# API 配置
VITE_API_BASE_URL=http://localhost:3001/api  # 开发：http://localhost:3001/api, 生产：/api 或完整域名
VITE_API_TIMEOUT=10000              # API 请求超时时间（毫秒）

# CDN 配置
VITE_CDN_URL=/                      # 静态资源 CDN 地址

# 调试模式
VITE_DEBUG=true                     # 开发：true, 生产：false
VITE_LOG_LEVEL=debug                # debug | info | warn | error

# 本地存储配置
VITE_LOCAL_STORAGE_PREFIX=musicvista_dev  # localStorage 键名前缀

# 功能开关（可选）
# VITE_ENABLE_ANALYTICS=false       # 是否启用分析
# VITE_GOOGLE_CLIENT_ID=            # Google OAuth（如需要）

# ===========================================
# 💡 开发环境 (.env.development) 示例：
# VITE_API_BASE_URL=http://localhost:3001/api
# VITE_DEBUG=true
# VITE_LOG_LEVEL=debug
#
# 💡 生产环境 (.env.production) 示例：
# VITE_API_BASE_URL=https://api.musicvista.com/api
# VITE_DEBUG=false
# VITE_LOG_LEVEL=warn
# ===========================================

