# ===========================================
# MusicVista Server 环境变量配置示例
# ===========================================
# 复制此文件为 .env 并填入实际值

# 运行环境
NODE_ENV=development          # 开发环境：development, 生产环境：production

# 服务器配置
PORT=3001                     # 服务器端口号
FRONTEND_URL=http://localhost:5173  # 前端应用地址（用于 CORS）

# JWT 认证配置（⚠️ 生产环境必须使用强随机密钥）
JWT_ACCESS_SECRET=your-access-secret-here     # 访问令牌密钥（建议64+字符）
JWT_REFRESH_SECRET=your-refresh-secret-here   # 刷新令牌密钥（建议64+字符）
JWT_ACCESS_EXPIRATION=15m     # 访问令牌过期时间
JWT_REFRESH_EXPIRATION=7d     # 刷新令牌过期时间

# PostgreSQL 数据库配置
DB_HOST=your-database-host.amazonaws.com  # 数据库主机地址
DB_PORT=5432                              # 数据库端口
DB_NAME=musicvista                        # 数据库名称
DB_USER=your-db-username                  # 数据库用户名
DB_PASSWORD=your-db-password              # 数据库密码

# 社交登录配置（可选）
GOOGLE_CLIENT_ID=                # Google OAuth 客户端 ID
GOOGLE_CLIENT_SECRET=            # Google OAuth 客户端密钥
FACEBOOK_APP_ID=                 # Facebook 应用 ID
FACEBOOK_APP_SECRET=             # Facebook 应用密钥
GITHUB_CLIENT_ID=                # GitHub OAuth 客户端 ID
GITHUB_CLIENT_SECRET=            # GitHub OAuth 客户端密钥

# 其他配置
LOG_LEVEL=info                   # 日志级别：debug, info, warn, error
MAX_REQUEST_SIZE=10mb            # 最大请求体大小
CORS_ORIGINS=http://localhost:5173  # 允许的跨域来源（多个用逗号分隔）

# ===========================================
# 💡 提示：
# 1. 生产环境请使用强随机密钥
#    生成方法：node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 2. 不要在公开仓库提交包含真实密钥的 .env 文件
# 3. 云平台部署时，在平台环境变量中配置这些值
# ===========================================
